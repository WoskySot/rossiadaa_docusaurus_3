# --------------------------- 依赖与构建产物 --------------------------------
/node_modules
/build
/.docusaurus
.cache-loader

# ---------------------- 助手工作数据与审查产物（不进仓库）-------------------
/.workbuddy
/review

# ---------------------- 改动前备份（不进仓库）-----------------------------
# 实施较大改动前按原路径镜像存放的副本；每个备份目录内有 MANIFEST.md 说明清单与还原方法
/backup-*

# --------------------------- 环境变量与密钥 -------------------------------
.env
.env.*
!.env.example
# EdgeOne CLI 的凭据文件：`edgeone login --local` 会把 API Token 明文写进
# <cwd>/.edgeone/auth.json，绝不能提交；CLI 也会主动读 ./.env 与 ./.env.local
# 里的 TENCENT_SECRET_ID / TENCENT_SECRET_KEY / EDGEONE_PAGES_API_TOKEN。
/.edgeone

# ------------------------------- 日志 ------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --------------------------- 编辑器 / 操作系统 ----------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/

# --------------------------- 各类缓存与临时文件 ---------------------------
*.tsbuildinfo
.eslintcache
